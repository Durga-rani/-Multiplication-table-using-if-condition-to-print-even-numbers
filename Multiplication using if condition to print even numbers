//multiplicatin using for loop to print even numbers
#include <stdio.h>

int main() 
{
    int n,i,prod;
    printf("enter a table name:");
    scanf("%d",&n);
    
   
    for (i=1;i<=10;i++)
    {
         if (i%2==0)
{
        prod=i*n;
        printf("%d*%d=%d\n",n,i,prod);
    }
}
    

    return 0;
}
Output:
enter a table name:12
12*2=24
12*4=48
12*6=72
12*8=96
12*10=120
